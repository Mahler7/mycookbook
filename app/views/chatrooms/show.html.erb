<%= render "shared/page_title", title: "Chef Chat" %>

<div class="row chat-container">
  <div class="col-md-8 center-block">
    <div class="messages" id="messages">
      <%= render @messages %> 
    </div> <!-- looks for message partial in messages folder and @messages from controller -->
    <hr>
    <div class="message-container">
      <%= form_for(@message, remote: true, html: { class: "form-horizontal", role: "form" } ) do |f| %>
        <%= f.text_area :content, class: "form-control", data: { behavior: "room_speaker" }, placeholder: "Say something!" %>
        <%= f.submit "Send", class: "btn btn-primary", data: { send: "messsage" } %>
      <% end %>
    </div>
  </div>
</div>